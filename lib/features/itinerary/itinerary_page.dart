import 'package:flutter/material.dart';

class ItineraryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("AI Saathi – Itinerary")),
      body: Center(child: Text("Day-by-day itinerary will appear here.")),
    );
  }
}
