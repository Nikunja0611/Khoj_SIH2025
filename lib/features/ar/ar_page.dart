import 'package:flutter/material.dart';

class ARPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Immersive AR Experience")),
      body: Center(child: Text("AR content loads here")),
    );
  }
}
